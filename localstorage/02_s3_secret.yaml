apiVersion: v1
kind: Secret
metadata:
  name: logging-loki-s3
  namespace: openshift-logging
stringData:
  access_key_id: minio
  access_key_secret: minioredhat
  bucketnames: s3-bucket-name
  endpoint: http://minio-disk.apps.mgmt2.npss.bos2.lab
